apiVersion: v1
kind: Pod
metadata:
  name: webapp-adam
  labels:
    app.kubernetes.io/name: webapp-adam
    app.kubernetes.io/instance: webapp-adam
spec:
  containers:
  - name: webapp
    image: asinatra/flask-app
    ports:
      - name: http
        containerPort: 5000
        protocol: TCP
  
